# Data Directory

This directory contains player statistics and event data.

Place your CSV/Excel files here:
- Player statistics from various leagues
- Event data from match tracking systems
- Any preprocessed datasets

Example data format:
- `players.csv`: Player statistics with columns like name, position, league, tackles, passes, etc.
- `events.csv`: Event-level match data
